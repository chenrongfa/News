<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="radio_style">
        <item name="android:textColor">@drawable/font_selector1</item>
        <item name="android:button">@null</item>
        <item name="android:layout_weight">1</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:layout_width">0dp</item>
        <item name="android:drawablePadding">5dp</item>
        <item name="android:gravity">center</item>
    </style>
    <style name="tv_home">
        <item name="android:gravity">center</item>
        <item name="android:clickable">true</item>
        <item name="android:layout_width">0dp</item>
        <item name="android:layout_weight">1</item>
        <item name="android:layout_height">wrap_content</item>
    </style>
    <style name="CustomTab" parent="Base.Widget.Design.TabLayout">
        <item name="tabBackground">@drawable/tab_bg</item>
        <item name="tabMaxWidth">100dp</item>

        <item name="tabPaddingEnd">30dp</item>
        <item name="tabIndicatorColor">@color/colorPrimaryDark</item>
        <item name="tabTextAppearance">@style/myTabTextSize</item>

    </style>
<style name="myTabTextSize" parent="TextAppearance.Design.Tab">
    <item name="android:textSize">16sp</item>


</style>


</resources>
